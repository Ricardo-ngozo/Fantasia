{
  "name": "fantasia",
  "version": "1.0.0",
  "private": true,
  "description": "A full-stack private messaging app for exactly two users.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
